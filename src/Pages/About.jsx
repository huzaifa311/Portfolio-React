import React from 'react';
import styles from '../Custom.module.css';

const Home = () => {
  return (
    <div className={`mt-[85px] flex justify-center text-5xl mb-64 font-semibold text-black ${styles.textShadow} ${styles.cntrlTop}`}>
      About
    </div>
  )
}

export default Home
